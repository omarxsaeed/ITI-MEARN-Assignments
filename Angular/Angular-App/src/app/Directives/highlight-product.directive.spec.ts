import { HighlightProductDirective } from './highlight-product.directive';

describe('HighlightProductDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightProductDirective();
    expect(directive).toBeTruthy();
  });
});
